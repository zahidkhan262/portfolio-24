import React from 'react'
import { motion } from 'framer-motion';
import { textVariants } from '../utils/motions'
const About = () => {
    return (
        <>
            <motion.div variants={textVariants()}>

            </motion.div>
        </>
    )
}

export default About